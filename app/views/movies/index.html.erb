<h1><%="Welcome #{current_user.username}!"%></h1>

<p><%= button_to "Find Top 20 Rated Movies", movies_path, method: :get %></p>
<% @top_20_movies.each do |movie| %>
  <li><%= "#{movie.title} vote average:#{movie.vote_average}" %></li>
  <% end %>
