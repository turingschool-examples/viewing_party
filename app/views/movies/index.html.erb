<%= render partial: "shared/search_form" %>
<p>
<% @movies.each do |movie| %>
  Title: <%= link_to "#{movie.title}", movie_path(movie.id) %>
  Vote Average Score: <%= movie.vote_average %> <br>
<% end %>
</p>
